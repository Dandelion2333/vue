1、js负责脚本数据处理
    1、通过new创建vue对象。
    2、data中包好基本的变量，元素
    3、method中可以包含函数。 
2、HTML负责显示数据
    1、head包含基本的信息，如定义字符类型，网页名称，引用库。
    2、body包含网页显示的数据
        1、写法上用div开头。
        2、可以直接使用ID对应的vue对象的变量、函数、方法。
3、两者通过定义共同的ID，从而联动起来。